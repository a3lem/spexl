---
name: project_active_work
description: Current project status - rewrite complete, now dogfooding spexl on itself (as of 2026-03-19)
type: project
---

The `rewrite-as-spexl` change is **fully implemented**. 130 tests pass. The transition from the `spec-driven-dev` Claude Code plugin to a standalone CLI is complete.

Current phase: **dogfooding**. spexl is installed and generating skills/agents for its own development workflow.

**Next step:** Archive `rewrite-as-spexl` via `spexl archive rewrite-as-spexl` once deltas are synced to reference specs.

**How to apply:** The project is past implementation. Focus is on using spexl in practice, finding rough edges, and archiving the foundational change.
